name 'libtirpc'
org 'amylum'

licenses 'COPYING'

deps(
  krb5: '1.15.1-1',
  openssl: '1.0.2l-1',
  'linux-api-headers': '4.12-1'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

patch 'Use-netbsd-queue.h.patch'
patch 'remove-des-functionality.patch'

build do
  run 'autoreconf -i'
  configure
  make
  install
end

test do
  # TODO: add tests
end
